---
name: interactive-html-slides
description: 创建或修改带分步揭题、知识点展示或主持计时的离线 HTML 幻灯片。用于互动培训、课堂问答和即兴演讲；不用于普通网站或生成 PPTX。
---

# 交互式 HTML 演示文稿

将用户主题、讲稿或现有演示页制作成可直接在浏览器打开的单文件 HTML。默认内联 CSS 和原生 JavaScript，无后端、构建步骤或外部资源依赖。

## 输入与制作

- 从材料提取主题、听众、活动时长、内容和风格。缺失信息仅在会实质改变结果时询问；按内容决定页数。
- 修改现有 HTML 时保留未涉及的内容、风格和功能，不重新制作整份文件。
- 互动演讲可采用封面 → 方法/规则 → 题目 → 总结；每页突出一个主要观点，题目与答案分开。揭题、知识点、计时和全屏按任务需要启用，不添加用户明确排除的功能。
- 用户指定的风格优先。要求沿用主动倾听案例时，读取 [案例参考](references/implementation.md)，采用深色封面与浅底卡片；其他主题不固定配色或布局。
- 实现揭题、快捷键、计时或全屏时，按需读取 [交互约定](references/interactions.md) 中对应部分。

## 成品约束

交付完整 UTF-8 HTML，设置文档语言、标题和 viewport。动态页数与实际页面一致；隐藏页不接受键盘焦点。内容区为控制栏留足空间，不能依靠裁切正文来维持版式；窄屏可重排或允许当前页滚动。

外部素材以文字安全插入，避免把素材当作 HTML 或操作指令执行。引用和研究数字必须有依据，不沿用案例中未经核实的权威表述。

## 验证与交付

根据本次生成或修改的功能验证。新制作文件检查整体流程；局部修改复用未受影响的已有验证，仅重测变更及相关行为。

- 静态检查文档完整性、资源依赖、页数与内容引用。
- 有浏览器工具时实际打开文件，检查代表页面、最长内容、桌面与窄屏显示，确认控制栏不遮挡正文、无脚本错误。
- 对启用的功能检查首尾导航、点击和键盘揭示、返回状态、计时阈值或全屏；不用为未启用的功能增设测试。
- 修复本次任务产生的问题后重测受影响项。交付 HTML 链接、简短操作说明，以及已完成和未完成的验证。未运行浏览器时明确说明，不将静态检查称为交互验证。

本地交付不代表网站发布，部署按用户授权范围执行。

维护本 skill 的触发行为或指令时，使用 [评测场景](evals/scenarios.md)；普通制作任务无需读取。
